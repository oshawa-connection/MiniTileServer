{
    "tasks": [
        {
            "label": "g++.exe build active file",
            "type": "cppbuild",
            "command": "/usr/bin/gcc",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "args": [
                "-g",
                "${workspaceFolder}/*/**.c",
                // "${workspaceFolder}/*/**.h",
                "-o",
                "helloWorld.out",
                
            ],
        },
    ],
    "version": "2.0.0"
}